CAPSULE INTERFACE CapScale ;
IMPORT ScaleTypes AS ST;
PORT p1 : PROTOCOL
 INCOMING MESSAGE set(v : ST.Volts; t : ST.Time) ;
 OUTGOING MESSAGE scaleOut(v : ST.Volts; t : ST.Time) ;
 INCOMING MESSAGE expect(v : INTEGER; t : INTEGER) ;

END;
END CapScale.
