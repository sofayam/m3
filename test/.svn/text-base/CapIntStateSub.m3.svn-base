CAPSULE CapIntStateSub ;
  IMPORT Timer;
  ACTIVITY windup () = 
  BEGIN 
    Timer.Start(t)
  END windup ;
  VAR t : PERIODIC FIXED TIMER DELAY 200 ms;
  CONNECT
    t -> alarm;

BEGIN

END CapIntStateSub.
