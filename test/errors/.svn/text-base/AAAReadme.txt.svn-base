These are tests of the semantic checking of the compiler. The expected error code is denoted in a pragma on
the line where it is expected. Currently we are happy if we can detect one per file reliably.

