import M3Types
import string

def Equal(t,u):
    return M3Types.M3Boolean.createObject(t.val == u.val)

def FromChars(a):
    return M3Types.M3Text.createObject(string.join([el.val for el in a.val],""))
